kind: pipeline
type: docker
name: default
steps:
  - name: fetch-fds-submodule
    image: alpine/git:v2.26.2
    commands:
      - git config submodule.fivium-design-system-core.url https://github.com/Fivium/fivium-design-system-core.git
      - git submodule update --init --recursive

  - name: build-fds
    image: node:18
    commands:
      - cd fivium-design-system-core
      - npm install
      - npx gulp buildAll
      - cd ..

  - name: build-frontend
    image: node:18
    commands:
      - npm install
      - npx gulp buildAll

  - name: build-backend
    image: eclipse-temurin:21-jdk
    commands:
      - chmod +x gradlew
      - ./gradlew test jacocoTestReport checkstyleMain bootWar bootJar

  # This is a plain branch analysis (sonar.branch.name), not PR decoration, so it doesn't need the
  # GitHub integration info that the github-pr pipeline's code-analysis step relies on. It exists to
  # keep SonarCloud's stored develop branch snapshot from going stale, since nothing else refreshes it
  # and every PR's "new code" comparison is diffed against that snapshot.
  - name: sonar-analysis
    image: eclipse-temurin:21-jdk
    environment:
      SONAR_TOKEN:
        from_secret: sonar_token
    commands:
      - chmod +x gradlew
      - ./gradlew sonarqube -info
    when:
      branch:
        - develop
      status:
        - success

  - name: publish-docker-develop
    image: plugins/docker:19.03.8
    settings:
      registry: quay.io
      repo: quay.io/fivium/pipeline-works-authorisations
      tags:
        - develop
        - develop-${DRONE_BUILD_NUMBER}
      config:
        from_secret: docker_config
    when:
      branch:
        - develop
      status:
        - success

  - name: publish-docker
    image: plugins/docker:19.03.8
    settings:
      registry: quay.io
      repo: quay.io/fivium/pipeline-works-authorisations
      tags:
        - ${DRONE_BRANCH//\//-}-${DRONE_BUILD_NUMBER}
      config:
        from_secret: docker_config
    when:
      branch:
        - master
        - release/**
        - hotfix/**
        - publish-to-docker
        - rebrand-to-nsta
      status:
        - success

  - name: trivy-build-image
    image: docker:19.03.8
    volumes:
      - name: docker-sock
        path: /var/run/docker.sock
    commands:
      - docker build -t quay.io/fivium/pipeline-works-authorisations:trivy-scan-target .

  - name: trivy-scan
    image: aquasec/trivy:0.57.0
    volumes:
      - name: docker-sock
        path: /var/run/docker.sock
      - name: drone-trivy-cache
        path: /root/trivy-cache
      - name: webfiles
        path: /var/webfiles
    environment:
      TRIVY_DB_REPOSITORY: public.ecr.aws/aquasecurity/trivy-db,aquasec/trivy-db,ghcr.io/aquasecurity/trivy-db
      TRIVY_JAVA_DB_REPOSITORY: public.ecr.aws/aquasecurity/trivy-java-db,aquasec/trivy-java-db,ghcr.io/aquasecurity/trivy-java-db
    commands:
      # timeout set as initial cache population can seemingly take a while sometimes
      - trivy image --timeout 30m --exit-code 1 quay.io/fivium/pipeline-works-authorisations:trivy-scan-target
    when:
      status:
        - success

#  - name: vulnerability-scan-java
#    image: eclipse-temurin:21-jdk
#    commands:
#      - chmod +x gradlew
#      - ./gradlew dependencyCheckAnalyze
#    volumes:
#      - name: nvd-db
#        path: /var/nvd-db
#    when:
#      branch: develop

  - name: vulnerability-scan-npm
    image: node:10.12.0
    commands:
      - npm audit > build/reports/npm-audit-report.txt || true # dont fail build if non 0 exit code
    when:
      branch: develop

  - name: sync-reports
    image: alpine:3.12.1
    commands:
      - mkdir -p /var/webfiles/${DRONE_BUILD_NUMBER}
      - cp -r build/reports /var/webfiles/${DRONE_BUILD_NUMBER}
    volumes:
      - name: webfiles
        path: /var/webfiles
    when:
      status: [ success, failure ]

  - name: scan-and-sync-public-repo
    image: quay.io/fivium/drone-trufflehog-plugin:v1.0
    settings:
      scan_since_commit_hash: 6065b98867ea96b00874ff205d8e54eda3f13ce5
      sync_to_mirror: true
      mirror_remote_url: git@github.com:North-Sea-Transition-Authority/pipeline-works-authorisations.git
      mirror_branch_to_sync: develop
      mirror_remote_ssh_key:
        from_secret: sync_ssh_key
    when:
      event:
        - push
      branch:
        - develop


  - name: slack-all
    image: plugins/slack:linux-amd64
    settings:
      webhook:
        from_secret: slack_webhook
      channel: oga-pipelines-builds
      template: |
        *{{build.status}}* <{{build.link}}|Commit {{truncate build.commit 7}} on {{build.branch}} by ${DRONE_COMMIT_AUTHOR_NAME}>
        Reports published to: http://drone-assets.fivium.local:9090/pwa/{{build.number}}/
    when:
      status: [ success, failure ]

trigger:
  event:
    include:
      - push

volumes:
  - name: webfiles
    host:
      path: /home/fivium/www/pwa

  - name: nvd-db
    host:
      path: /home/fivium/nvd-db



  - name: docker-sock
    host:
      path: /var/run/docker.sock

  - name: drone-trivy-cache
    host:
      path: /root/.cache/

---
kind: pipeline
type: docker
name: github-pr

clone:
  disable: true

steps:
  - name: clone
    image: alpine/git:v2.26.2
    commands:
      # Manually cloning as there seems to be a bug with DRONE_BRANCH on Pull Request Events
      # DRONE_BRANCH is set to the target branch, rather than the current branch
      # This means that Sonarcloud doesn't think anything has changed
      - git clone https://github.com/Fivium/pipeline-works-authorisations.git .
      - git checkout $DRONE_SOURCE_BRANCH

  - name: restore-cache
    image: drillster/drone-volume-cache
    volumes:
      - name: drone-cache
        path: /cache
    settings:
      restore: true
      mount:
        - ./node_modules
        # We can only mount volume caches into the working directory, so all steps which use Gradle must have
        # the GRADLE_USER_HOME environment variable pointing here.
        - ./gradle_cache
        - ./sonar_cache

  - name: fetch-fds-submodule
    image: alpine/git:v2.26.2
    commands:
      - git config submodule.fivium-design-system-core.url https://github.com/Fivium/fivium-design-system-core.git
      - git submodule update --init --recursive

  - name: build-fds
    image: node:18
    commands:
      - cd fivium-design-system-core
      - npm install
      - npx gulp build
      - cd ..

  - name: build-frontend
    image: node:18
    commands:
      - npm install
      - npx gulp buildAll

  - name: build-java
    image: eclipse-temurin:21-jdk
    environment:
      GRADLE_USER_HOME: /drone/src/gradle_cache
    commands:
      - chmod +x gradlew
      - ./gradlew test jacocoTestReport checkstyleMain bootWar bootJar

  # This is in the PR pipeline as only PR events get the necessary information for proper github integration
  - name: code-analysis
    image: eclipse-temurin:21-jdk
    environment:
      SONAR_TOKEN:
        from_secret: sonar_token
      # This SONAR_USER_HOME is the cache path - not the actual user home
      SONAR_USER_HOME: /drone/src/sonar_cache
    commands:
      - chmod +x gradlew
      - ./gradlew sonarqube -info

  - name: rebuild-cache
    image: drillster/drone-volume-cache
    volumes:
      - name: drone-cache
        path: /cache
    settings:
      rebuild: true
      mount:
        - ./node_modules
        - ./gradle_cache
        - ./sonar_cache

trigger:
  event:
    - pull_request

volumes:
  - name: drone-cache
    host:
      path: /home/fivium/drone-cache

---

kind: secret
name: docker_config
data: 

---

kind: secret
name: sonar_token
data: aW0siut+zLoCoy1mihvxveYJtOj2OFXa6vd8Dwy7NJ97XFDr9lGQiwrbkIZHns/9Z8Jst+n/1niImcIfg9sgyj9pFdSHQkWszbINzPV/1Q+Pg1/SDMzqqCNaV2Y=

---

kind: secret
name: slack_webhook
data: Tm25dzBnG4pzE8LN/DYTumOfEx6jEnod6UGxGA2SMhIjkngk97+j4OhqMcGLBmpJjATCmMGhvYlL8Zw0fvP/I6DUHmeQgDA0Zk9DxK427A4HMYO8vhzi7rYnPCUNKwQdFiJZr9ROy6As5W0=

---

kind: secret
name: sync_ssh_key
data: 
